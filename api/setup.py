from setuptools import setup, find_packages

setup(
    name='api',
    version='0.1.0',
    packages=find_packages(),
    description='The API library defines the data model and simplifies communication between the platform and plug-ins',
)